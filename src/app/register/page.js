import React from 'react';
import Register from "./register";

export const metadata = {
  title: 'Register: Dashboard Management',
}

const RegisterPage= () => {
  return <Register />
}

export default RegisterPage;